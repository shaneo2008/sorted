/**
 * TODO(M2): "Owed to you" — GET /payments?status=pending.
 * Each row: client · service · amount · [Send link] [Mark paid] buttons.
 * Header stat: total outstanding. Second tab/section: recent paid.
 * TODO(M6): add "This year" summary strip (GET /reports/summary) + Export.
 */
export function Money() {
  return (
    <div>
      <h1>Money</h1>
      <p className="muted">Not implemented — see TODO(M2).</p>
    </div>
  );
}
